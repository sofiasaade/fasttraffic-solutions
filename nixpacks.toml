# Railway build config for Fast Traffic OS
[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile=false"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
